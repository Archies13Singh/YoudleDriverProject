<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/bottom_nav"
    app:startDestination="@id/user">

    <fragment android:id="@+id/user"
        android:label="User"
        android:name="com.example.driverapp.ui.user.UserFragment"
        tools:layout="@layout/fragment_user"/>

    <fragment android:id="@+id/home"
        android:label="Home"
        android:name="com.example.driverapp.ui.home.HomeFragment"
        tools:layout="@layout/fragment_home"/>

    <fragment android:id="@+id/history"
        android:label="History"
        android:name="com.example.driverapp.ui.history.HistoryFragment"
        tools:layout="@layout/fragment_history"/>
</navigation>